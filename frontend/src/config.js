/**
 * Global frontend configuration settings.
 */
export const CONFIG = {
  // Set to true to require 100% completion of the active track before allowing domain switches
  ENFORCE_TRACK_COMPLETION_LOCK: true,
};
